---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Home">

  <!-- ===== HERO SECTION ===== -->
  <section class="relative overflow-hidden bg-mesh-hero min-h-[90vh] flex items-center">
    <!-- LAYER 1: Dot grid with radial fade mask -->
    <div
      class="absolute inset-0 pattern-dots opacity-40 pointer-events-none"
      style="mask-image: radial-gradient(ellipse 70% 70% at 60% 50%, black 20%, transparent 70%); -webkit-mask-image: radial-gradient(ellipse 70% 70% at 60% 50%, black 20%, transparent 70%);"
      aria-hidden="true"
    ></div>

    <!-- LAYER 2: Gradient orbs -->
    <div class="gradient-orb-lg w-[600px] h-[600px] -top-40 -right-40 opacity-60" aria-hidden="true"></div>
    <div class="gradient-orb w-[400px] h-[400px] bottom-0 left-[-100px] opacity-40" aria-hidden="true"></div>
    <div class="gradient-orb w-[250px] h-[250px] top-[40%] right-[25%] opacity-20" aria-hidden="true"></div>

    <!-- LAYER 2b: Decorative circles -->
    <div class="deco-circle-outline w-[300px] h-[300px] top-20 right-[15%] opacity-10" aria-hidden="true"></div>
    <div class="deco-circle w-16 h-16 top-[30%] right-[10%] animate-float opacity-30" aria-hidden="true"></div>
    <div class="deco-circle-outline w-24 h-24 bottom-[20%] left-[8%] animate-float opacity-15" style="animation-delay: 1.5s;" aria-hidden="true"></div>

    <!-- LAYER 2c: Decorative SVG shapes -->
    <svg class="absolute top-16 left-[12%] w-12 h-12 text-accent opacity-15 animate-spin-slow" viewBox="0 0 48 48" fill="none" aria-hidden="true">
      <rect x="4" y="4" width="40" height="40" rx="4" stroke="currentColor" stroke-width="1.5" transform="rotate(12 24 24)" />
      <rect x="12" y="12" width="24" height="24" rx="2" stroke="currentColor" stroke-width="1" transform="rotate(-6 24 24)" />
    </svg>
    <svg class="absolute bottom-32 right-[20%] w-16 h-16 text-accent opacity-10 animate-float" style="animation-delay: 2s;" viewBox="0 0 64 64" fill="none" aria-hidden="true">
      <circle cx="32" cy="32" r="28" stroke="currentColor" stroke-width="1" />
      <circle cx="32" cy="32" r="16" stroke="currentColor" stroke-width="1" stroke-dasharray="4 4" />
      <circle cx="32" cy="32" r="4" fill="currentColor" opacity="0.4" />
    </svg>
    <svg class="absolute top-[45%] left-[5%] w-8 h-8 text-accent opacity-10" viewBox="0 0 32 32" fill="none" aria-hidden="true">
      <path d="M16 2L30 16L16 30L2 16Z" stroke="currentColor" stroke-width="1" />
    </svg>

    <!-- LAYER 3: Hero content -->
    <div class="container-wide relative z-10 py-24 md:py-32">
      <div class="max-w-4xl">
        <div class="animate-fade-in">
          <span class="badge-accent mb-6 inline-block">Daniel Brady</span>
        </div>

        <h1 class="text-display text-ink mb-8 animate-slide-up animate-delay-1">
          RevOps built the workflows.<br />
          <span class="text-gradient">Agentic Ops</span> deploys<br />
          the agents that run them.
        </h1>

        <p class="text-lg md:text-xl text-ink-light max-w-2xl mb-12 animate-slide-up animate-delay-2 leading-relaxed">
          I build and deploy AI agents across the GTM stack — prospecting, enrichment,
          content, and reporting. Most teams are still figuring out where to start.
          I'm already in production.
        </p>

        <!-- Newsletter signup -->
        <div class="animate-slide-up animate-delay-3">
          <div class="flex flex-col sm:flex-row gap-3 max-w-lg">
            <input
              type="email"
              placeholder="you@company.com"
              class="flex-1 px-5 py-3.5 bg-white/80 backdrop-blur-sm border border-border rounded-xl text-sm focus:outline-none focus:border-accent focus:ring-2 focus:ring-accent/20 transition-all shadow-sm"
            />
            <button class="btn btn-gradient btn-lg btn-glow press-scale">
              Subscribe
            </button>
          </div>
          <p class="text-xs text-ink-lighter mt-3 font-mono">
            Join the Agentic Ops newsletter. No spam, unsubscribe anytime.
          </p>
        </div>
      </div>
    </div>

    <!-- Bottom fade to next section -->
    <div class="absolute bottom-0 left-0 right-0 h-24 bg-gradient-to-t from-cream to-transparent pointer-events-none" aria-hidden="true"></div>
  </section>


  <!-- ===== SECTION SEPARATOR ===== -->
  <div class="section-separator"></div>


  <!-- ===== WHAT IS AGENTIC OPS? ===== -->
  <section class="section-lg relative overflow-hidden">
    <!-- Gradient line at top -->
    <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-accent/30 to-transparent" aria-hidden="true"></div>

    <!-- Subtle radial accent -->
    <div class="gradient-orb-lg w-[500px] h-[500px] -top-60 -left-40 opacity-30" aria-hidden="true"></div>

    <div class="container-wide relative z-10 reveal-on-scroll">
      <!-- Asymmetric layout: 7/5 split -->
      <div class="grid md:grid-cols-12 gap-8 md:gap-16 items-start">

        <!-- Left: text content (takes 7 cols) -->
        <div class="md:col-span-7">
          <div class="accent-bar mb-6"></div>
          <p class="text-label mb-4">The concept</p>
          <h2 class="text-display-sm text-ink mb-6 text-balance">
            What is <span class="text-gradient">Agentic Ops</span>?
          </h2>
          <p class="text-lg text-ink-light mb-8 leading-relaxed max-w-xl">
            Agentic Ops is the next evolution of revenue operations. Instead of building
            workflows and hoping humans follow them, you deploy autonomous AI agents that
            actually execute the work — from prospecting to data enrichment to reporting.
          </p>
          <a href="/agentic-ops" class="btn btn-secondary btn-glow group">
            Read the full manifesto
            <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
        </div>

        <!-- Right: evolution card (takes 5 cols) -->
        <div class="md:col-span-5">
          <div class="card-gradient corner-accent p-8">
            <p class="text-label mb-6">The evolution</p>
            <div class="space-y-6">
              <div class="flex items-start gap-4">
                <span class="font-mono text-xs text-ink-lighter mt-1 w-6 shrink-0">01</span>
                <div>
                  <p class="font-medium text-sm mb-1">Manual Ops</p>
                  <p class="text-sm text-ink-light leading-relaxed">Humans do everything. Spreadsheets, copy-paste, tribal knowledge.</p>
                </div>
              </div>

              <hr class="hr-gradient" />

              <div class="flex items-start gap-4">
                <span class="font-mono text-xs text-ink-lighter mt-1 w-6 shrink-0">02</span>
                <div>
                  <p class="font-medium text-sm mb-1">RevOps</p>
                  <p class="text-sm text-ink-light leading-relaxed">Workflows, automations, integrations. Humans still in the loop.</p>
                </div>
              </div>

              <hr class="hr-gradient" />

              <div class="flex items-start gap-4">
                <span class="font-mono text-xs text-accent mt-1 w-6 shrink-0">03</span>
                <div>
                  <p class="font-medium text-sm mb-1 text-accent">Agentic Ops</p>
                  <p class="text-sm text-ink-light leading-relaxed">AI agents execute autonomously. Humans supervise and strategize.</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>


  <!-- ===== AGENT STACK BENTO GRID (DARK SECTION) ===== -->
  <section class="section-lg section-dark relative overflow-hidden">
    <!-- Gradient line at top -->
    <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-accent/30 to-transparent" aria-hidden="true"></div>

    <!-- Dark pattern background -->
    <div class="absolute inset-0 pattern-dots-dark opacity-60 pointer-events-none" aria-hidden="true"></div>

    <!-- Gradient orbs -->
    <div class="gradient-orb w-[300px] h-[300px] top-20 right-[10%] opacity-20" aria-hidden="true"></div>
    <div class="gradient-orb-lg w-[500px] h-[500px] bottom-0 left-[-100px] opacity-15" aria-hidden="true"></div>
    <div class="gradient-orb w-[200px] h-[200px] bottom-[30%] right-[30%] opacity-10" aria-hidden="true"></div>

    <div class="container-wide relative z-10">
      <div class="text-center mb-16 reveal-on-scroll">
        <div class="accent-bar-wide mx-auto mb-6"></div>
        <p class="text-label mb-4">The framework</p>
        <h2 class="text-display-sm text-gradient-light mb-6">The GTM Agent Stack</h2>
        <p class="text-lg max-w-2xl mx-auto" style="color: #b0b0b0;">
          Four layers of AI agents across your go-to-market motion. Each one replaces
          hours of manual work with autonomous execution.
        </p>
      </div>

      <!-- Bento grid: true asymmetric with row-span -->
      <div class="grid grid-cols-1 md:grid-cols-3 md:grid-rows-[auto_auto] gap-4 md:gap-5 reveal-on-scroll">

        <!-- Prospecting — spans 2 columns, row 1 (featured card) -->
        <div class="md:col-span-2 card-dark card-bento p-8 md:p-10 group relative overflow-hidden" style="border-radius: var(--radius-lg);">
          <div class="absolute top-0 right-0 w-1/2 h-full opacity-5 pattern-lines-dark pointer-events-none" aria-hidden="true"></div>
          <div class="relative z-10">
            <div class="flex items-start justify-between gap-6 mb-6">
              <div class="w-12 h-12 rounded-xl bg-accent/10 border border-accent/20 flex items-center justify-center group-hover:border-accent/40 transition-colors">
                <svg class="w-6 h-6 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="10" />
                  <circle cx="12" cy="12" r="6" />
                  <circle cx="12" cy="12" r="2" />
                  <line x1="12" y1="2" x2="12" y2="5" />
                  <line x1="12" y1="19" x2="12" y2="22" />
                  <line x1="2" y1="12" x2="5" y2="12" />
                  <line x1="19" y1="12" x2="22" y2="12" />
                </svg>
              </div>
              <!-- Oversized stat -->
              <div class="text-right hidden sm:block">
                <span class="text-5xl md:text-6xl font-serif text-gradient leading-none">24/7</span>
                <p class="text-xs mt-1" style="color: #666;">always-on prospecting</p>
              </div>
            </div>
            <h3 class="text-2xl md:text-3xl mb-3 text-cream">Prospecting</h3>
            <p class="text-base leading-relaxed max-w-lg" style="color: #b0b0b0;">
              Agents that find, qualify, and prioritize leads autonomously. No more manual
              list building. Define your ICP once, let agents continuously surface your best
              opportunities from across the web.
            </p>
            <div class="mt-6 flex items-center gap-2 flex-wrap">
              <span class="badge-dark">ICP Matching</span>
              <span class="badge-dark">Lead Scoring</span>
              <span class="badge-dark">Auto-Qualification</span>
            </div>
          </div>
        </div>

        <!-- Enrichment — spans 1 col, 2 rows (tall card) -->
        <div class="md:row-span-2 card-dark card-bento p-8 group flex flex-col justify-between relative overflow-hidden" style="border-radius: var(--radius-lg);">
          <div class="absolute inset-0 opacity-5 pattern-dots-dark pointer-events-none" aria-hidden="true"></div>
          <div class="relative z-10">
            <div class="w-12 h-12 rounded-xl bg-accent/10 border border-accent/20 flex items-center justify-center mb-6 group-hover:border-accent/40 transition-colors">
              <svg class="w-6 h-6 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <rect x="2" y="3" width="20" height="5" rx="2" />
                <rect x="2" y="10" width="20" height="5" rx="2" />
                <rect x="2" y="17" width="20" height="5" rx="2" />
                <circle cx="17" cy="5.5" r="1" fill="currentColor" />
                <circle cx="17" cy="12.5" r="1" fill="currentColor" />
                <circle cx="17" cy="19.5" r="1" fill="currentColor" />
              </svg>
            </div>
            <h3 class="text-xl mb-3 text-cream">Enrichment</h3>
            <p class="text-sm leading-relaxed" style="color: #b0b0b0;">
              Agents that clean, enrich, route, and score your CRM data in real time.
              Always fresh, always accurate.
            </p>
          </div>
          <!-- Oversized stat at bottom of tall card -->
          <div class="relative z-10 mt-8 pt-6 border-t border-white/5">
            <span class="text-5xl md:text-6xl font-serif text-gradient leading-none">99%</span>
            <p class="text-xs mt-1" style="color: #666;">data accuracy, always current</p>
          </div>
        </div>

        <!-- Row 2: Content + Reporting side by side under Prospecting -->

        <!-- Content -->
        <div class="card-dark card-bento p-8 group" style="border-radius: var(--radius-lg);">
          <div class="w-12 h-12 rounded-xl bg-accent/10 border border-accent/20 flex items-center justify-center mb-6 group-hover:border-accent/40 transition-colors">
            <svg class="w-6 h-6 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8l-6-6z" />
              <polyline points="14 2 14 8 20 8" />
              <line x1="8" y1="13" x2="16" y2="13" />
              <line x1="8" y1="17" x2="12" y2="17" />
            </svg>
          </div>
          <h3 class="text-xl mb-3 text-cream">Content</h3>
          <p class="text-sm leading-relaxed" style="color: #b0b0b0;">
            Agents that draft, personalize, and send communications at scale.
            Human quality, machine speed.
          </p>
          <div class="mt-6">
            <span class="text-3xl font-serif text-gradient leading-none">10x</span>
            <p class="text-xs mt-1" style="color: #666;">output velocity</p>
          </div>
        </div>

        <!-- Reporting -->
        <div class="card-dark card-bento p-8 group relative overflow-hidden" style="border-radius: var(--radius-lg);">
          <div class="absolute bottom-0 left-0 w-full h-1/2 opacity-5 pattern-lines-dark pointer-events-none" aria-hidden="true"></div>
          <div class="relative z-10">
            <div class="w-12 h-12 rounded-xl bg-accent/10 border border-accent/20 flex items-center justify-center mb-6 group-hover:border-accent/40 transition-colors">
              <svg class="w-6 h-6 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <rect x="3" y="3" width="18" height="18" rx="2" />
                <polyline points="7 14 10 10 13 13 17 8" />
                <line x1="17" y1="8" x2="17" y2="11" />
                <line x1="17" y1="8" x2="14" y2="8" />
              </svg>
            </div>
            <h3 class="text-xl mb-3 text-cream">Reporting</h3>
            <p class="text-sm leading-relaxed" style="color: #b0b0b0;">
              Agents that pull data, build reports, and surface insights before you even ask.
              Always-on analytics.
            </p>
            <div class="mt-6">
              <span class="text-3xl font-serif text-gradient leading-none">0 min</span>
              <p class="text-xs mt-1" style="color: #666;">time to insight</p>
            </div>
          </div>
        </div>
      </div>

      <!-- CTA for stack page -->
      <div class="text-center mt-12 reveal-on-scroll">
        <a href="/agent-stack" class="btn btn-gradient btn-lg btn-glow group">
          Explore the full Agent Stack
          <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>
    </div>
  </section>


  <!-- ===== RECENT WRITING ===== -->
  <section class="section-lg relative overflow-hidden">
    <!-- Gradient line at top -->
    <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-accent/30 to-transparent" aria-hidden="true"></div>

    <div class="gradient-orb-lg w-[400px] h-[400px] top-0 right-[-100px] opacity-20" aria-hidden="true"></div>

    <div class="container-wide relative z-10">
      <div class="flex items-end justify-between mb-12 reveal-on-scroll">
        <div>
          <div class="accent-bar mb-6"></div>
          <p class="text-label mb-4">Latest</p>
          <h2 class="text-display-sm">Writing</h2>
        </div>
        <a href="/writing" class="btn btn-secondary group hidden sm:inline-flex">
          View all
          <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>

      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6 stagger-children reveal-on-scroll">
        <!-- Card 1 -->
        <a href="/writing/deploying-your-first-gtm-agent" class="card-hover p-6 group block">
          <span class="badge-accent mb-4 inline-block">Playbook</span>
          <h3 class="text-xl mb-3 group-hover:text-accent transition-colors">
            Deploying Your First GTM Agent: A Practical Guide
          </h3>
          <p class="text-sm text-ink-light leading-relaxed mb-4">
            Most teams overcomplicate this. Here's how to go from zero to a deployed agent in a week.
          </p>
          <span class="text-label">Feb 2026</span>
        </a>

        <!-- Card 2 -->
        <a href="/writing/revops-to-agentic-ops" class="card-hover p-6 group block">
          <span class="badge-accent mb-4 inline-block">Strategy</span>
          <h3 class="text-xl mb-3 group-hover:text-accent transition-colors">
            From RevOps to Agentic Ops: Why the Transition is Inevitable
          </h3>
          <p class="text-sm text-ink-light leading-relaxed mb-4">
            The same forces that created RevOps are now making it obsolete. Here's what comes next.
          </p>
          <span class="text-label">Jan 2026</span>
        </a>

        <!-- Card 3 -->
        <a href="/writing/agent-tools-landscape" class="card-hover p-6 group block">
          <span class="badge-accent mb-4 inline-block">Review</span>
          <h3 class="text-xl mb-3 group-hover:text-accent transition-colors">
            The AI Agent Tools Landscape: What Actually Works
          </h3>
          <p class="text-sm text-ink-light leading-relaxed mb-4">
            I've tested dozens of agent platforms. Here's an honest breakdown of what delivers and what doesn't.
          </p>
          <span class="text-label">Jan 2026</span>
        </a>
      </div>

      <!-- Mobile "View all" link -->
      <div class="mt-8 sm:hidden text-center">
        <a href="/writing" class="btn btn-secondary group">
          View all writing
          <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>
    </div>
  </section>


  <!-- ===== SECTION SEPARATOR ===== -->
  <div class="section-separator"></div>


  <!-- ===== BOTTOM CTA ===== -->
  <section class="section-lg relative overflow-hidden bg-gradient-warm">
    <!-- Gradient line at top -->
    <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-accent/30 to-transparent" aria-hidden="true"></div>

    <!-- Decorative elements -->
    <div class="gradient-orb w-[400px] h-[400px] top-[-100px] left-[20%] opacity-30" aria-hidden="true"></div>
    <div class="gradient-orb-lg w-[500px] h-[500px] bottom-[-150px] right-[10%] opacity-20" aria-hidden="true"></div>
    <div class="deco-circle-outline w-40 h-40 top-12 right-[8%] opacity-10 animate-float" aria-hidden="true"></div>
    <div class="deco-circle w-8 h-8 bottom-24 left-[12%] opacity-20 animate-float" style="animation-delay: 1s;" aria-hidden="true"></div>
    <div class="absolute inset-0 pattern-dots-sparse opacity-20 pointer-events-none" aria-hidden="true"></div>

    <div class="container-narrow relative z-10 text-center reveal-on-scroll">
      <div class="section-separator-accent mx-auto mb-8"></div>
      <h2 class="text-display-sm text-ink mb-6 text-balance">Stay ahead of the curve</h2>
      <p class="text-lg text-ink-light mb-10 max-w-lg mx-auto leading-relaxed">
        Get practical frameworks, agent deployment playbooks, and tool reviews —
        direct from someone who's actually building this.
      </p>

      <!-- Prominent subscribe form -->
      <div class="max-w-md mx-auto glow-behind">
        <div class="flex flex-col sm:flex-row gap-3 p-2 bg-white/70 backdrop-blur-sm rounded-2xl border border-border shadow-lg">
          <input
            type="email"
            placeholder="you@company.com"
            class="flex-1 px-5 py-3.5 bg-transparent text-sm focus:outline-none"
          />
          <button class="btn btn-gradient btn-glow press-scale whitespace-nowrap">
            Subscribe
          </button>
        </div>
      </div>

      <p class="text-sm text-ink-light mt-6 font-medium">
        Join 1,000+ GTM operators getting the Agentic Ops newsletter.
      </p>
      <p class="text-xs text-ink-lighter mt-1 font-mono">
        No spam, unsubscribe anytime.
      </p>
    </div>
  </section>

</BaseLayout>
